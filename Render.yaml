services:
  - type: worker
    name: whatsapp-ai-bot
    env: node
    rootDir: whatsapp-ai-bot
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production