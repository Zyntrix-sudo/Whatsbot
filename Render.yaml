services:
  - type: worker
    name: whatsapp-ai-bot
    env: node
    buildCommand: cd whatsapp-ai-bot && npm install
    startCommand: cd whatsapp-ai-bot && npm start
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production